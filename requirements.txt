# Hugging Face 튜토리얼 필수 라이브러리

# 핵심 라이브러리
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.15.0

# 딥러닝 프레임워크
torch>=2.0.0
torchvision>=0.15.0

# 학습 및 최적화
accelerate>=0.24.0
evaluate>=0.4.0
peft>=0.6.0

# 데이터 처리
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# 유틸리티
tqdm>=4.65.0
huggingface-hub>=0.19.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# 시각화 (선택)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0

# 모니터링 (선택)
tensorboard>=2.14.0
wandb>=0.15.0

# 웹 인터페이스 (선택)
gradio>=4.0.0
streamlit>=1.28.0

# Jupyter 지원 (선택)
jupyter>=1.0.0
ipywidgets>=8.1.0

# 추가 도구
requests>=2.31.0
beautifulsoup4>=4.12.0

# Vision 및 이미지 처리
Pillow>=10.0.0
opencv-python>=4.8.0  # 일반 시스템용
# opencv-python-headless>=4.8.0  # 서버/라즈베리파이용 (GUI 없음)
timm>=0.9.0  # PyTorch Image Models

# 라즈베리파이 Vision 지원 (선택)
# 라즈베리파이에서만 필요:
# PyGObject>=3.42.0  # GStreamer Python 바인딩 (시스템 패키지로 설치 권장)
# onnxruntime>=1.16.0  # ONNX 추론 엔진 (성능 향상)
# optimum>=1.13.0  # Hugging Face 최적화 도구

# 웹 스트리밍 (선택)
Flask>=3.0.0
psutil>=5.9.0  # 시스템 모니터링
